const cipher = {
  encode(offsetNum, inputStr){
    let menscriP = ""
    console.log(encode) 
  }

  decode(offsetNum, inputStr){
    let mensdecriP = ""
    console.log(decode)

};

export default cipher;
